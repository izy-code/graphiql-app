export enum LocalStorageKeys {}

export enum NonProtectedPaths {
  SIGN_UP = '/sign-up',
  SIGN_IN = '/sign-in',
}

export enum ProtectedPaths {
  REST = '/rest',
  GRAPHIQL = '/graphiql',
  HISTORY = '/history',
}
